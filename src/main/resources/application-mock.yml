spring:
  application:
    name: ChainCheck (Mock)
  task:
    scheduling:
      enabled: false

server:
  port: 8080

rpc:
  mode: mock
  mode-type: cosmos
  title: "Mock RPC"
  title-color: "#777777"
  get-safe-blocks: true
  get-finalized-blocks: true
  consensus:
    node-key: "mock-node-two"
  nodes:
    - name: Mock Node
      http: "http://mock.local"
      ws: "ws://mock.local"
      requests: sparse
      anomaly-detection:
        high-latency: 2000
    - name: Mock Node Two
      http: "http://mock-two.local"
      ws: "ws://mock-two.local"
      requests: sparse
      anomaly-detection:
        high-latency: 2600
